import { GetGeneratorFromIdPipe } from './get-generator-from-id.pipe';

describe('GetGeneratorFromIdPipe', () => {
  it('create an instance', () => {
    const pipe = new GetGeneratorFromIdPipe();
    expect(pipe).toBeTruthy();
  });
});
