import { GetWordFromIdPipe } from './get-word-from-id.pipe';

describe('GetWordFromIdPipe', () => {
  it('create an instance', () => {
    const pipe = new GetWordFromIdPipe();
    expect(pipe).toBeTruthy();
  });
});
